plugin: amazon.aws.aws_ec2
regions:
  - us-east-1


# filters:
#   tag:Name: dev*
  
keyed_groups: 
  - key: tags 
  - key: instance_type

  # - hosts: Name_dev1 = you can run certain commands on chosen VMs
